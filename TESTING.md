# 验证记录

初版验证（2026-09-04）：

- Windows、Node 24.19：15项离线自动测试通过，包括CLI完整离线流程、中文路径、原话匹配、时间边界、缺失互动数据、重复输出保护和上游失败传播。
- skill-creator quick_validate：通过。
- 新安装mcp-video-analyzer 0.10.0：CLI入口可运行。
- 合成2秒无声色块视频：实际经过上游引擎，生成1张去重关键帧，成功进入证据包；OCR流程运行，无可识别文字符合素材预期。
- 没有使用用户视频做公开测试，没有进行云ASR测试，也未证明所有TikTok链接能访问。

GitHub Actions配置用于后续Linux/Windows离线回归；其远端运行结果以仓库Actions页面为准。结构校验不证明语义正确；抽帧可用不意味着AI真实观看或事实判断必然正确。

本地安装使用pnpm；常规用户可按README使用npm。若使用pnpm且禁用了依赖安装脚本，需要依照包管理器提示启用可信依赖ffmpeg-static的安装脚本，否则可能缺少ffmpeg二进制。
